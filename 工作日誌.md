# 工作日誌

## 2026-07-04

- 更新 rule.txt，新增工作流程規則（工作日誌/對話紀錄/交接文檔/README 生成規則）
- 確認交接文檔採每次覆蓋更新（非累加歷史）
- 訪談確認專案目標：格狀月班表 Excel 轉換為每日橫向長條圖排班表 Excel
- 檢視現有手動版 Excel（`班表-7月.xlsx`），拆解出既有轉換邏輯：
  - 「整月外場班表」為格狀原始輸入
  - 「外場轉化」用陣列公式將上下班時間轉字串
  - 「內場轉化」公式為 `#REF!`（已損壞，來源表不存在於檔案中）
  - 分頁「1」~「31」用 INDIRECT 依列號指標撈每日每人上下班時間，餵給橫向長條圖
  - 發現既有檔案分頁「1」有列間距公式錯誤（C16 誤用 C12+3）
- 確認處理範圍：外場+內場皆需轉換
- 確認輸出圖表形式：Excel 原生圖表物件（非條件式格式模擬）
- 確認內場原始格式與外場「整月外場班表」完全相同，僅人名不同
- 確認上傳檔案為單一 Excel，同時包含外場、內場兩張原始表
- 建議技術路線：Python 後端（FastAPI/Flask + openpyxl），因純前端 JS 生態難以穩定產生 Excel 原生圖表物件
- 建立工作日誌、對話紀錄、交接文檔、README 四份檔案

### 第二輪：專案架構討論（泛用性辨識機制）

- 使用者提出泛用性需求：不同公司/品牌的 Excel 欄位配置不固定，員工姓名/日期/員工編號/職稱（主要特徵）位置需要辨識機制；另有例行行銷/節慶行銷/單店活動（次要特徵）需依日期填入輸出
- 建議機制：啟發式猜測（關鍵字詞庫+型態嗅探）+ 使用者確認畫面（下拉選單修正），不追求全自動辨識；並指出這是業界標準做法（Mailchimp/Stripe/銀行對帳單匯入工具模式）
- 確認次要特徵原樣依日期帶入輸出即可，不需要分類標籤
- 確認新增 JSON 儲存層存欄位對照範本，範本歸屬由使用者上傳時手動選擇（不做自動指紋比對配對）
- 更新交接文檔：架構從「無狀態轉換工具」變為「含辨識確認 UI + 範本持久化儲存」的工具，下一步行動清單相應擴充

## 2026-07-05

### 試作驗證：手動補完 `班表-7月` 範例檔案的工作頁1~31

- 使用者將參考檔案由 `班表-7月.xlsx` 轉存為 `班表-7月.ods`，要求先在工作頁2試作，驗證PIC1/PIC2呈現的轉換邏輯是否可行
- 建立副本 `班表-7月-副本.ods` 進行所有修改，原始檔全程未被觸碰
- 工作頁2：發現實際缺少17列員工資料（第18~34列，非原先以為的1列），且姓名欄公式 `_xlfn.IFS` 在此ODS中會產生 `#NAME?` 錯誤，圖表物件（Object 3/4）在檔案中完全是空殼、manifest未註冊——這三個問題都在使用者確認後一併處理：補齊29人資料、修正姓名公式為 `IF(...)`（保留PT字尾邏輯）、複製工作頁1的圖表定義並retarget重建
- 依相同標準批次處理工作頁3~31（同樣缺17列、同樣的姓名公式錯誤、同樣的空殼圖表），全部29個工作頁一次性修復完成
- 圖表座標軸顯示數字（1~29）而非姓名的問題，排查了三輪才根治：
  1. 第一輪誤判為需要重新計算儲存格（Ctrl+Shift+F9），但使用者鍵盤F9被系統設為飛航模式快捷鍵，改引導使用選單路徑（LibreOffice與OpenOffice選單位置不同，需視使用者實際軟體版本調整）
  2. 第二輪發現圖表另外內建一份「local-table」快取快照才是實際渲染依據，儲存格重算不會同步更新，需直接改寫快取內容
  3. 第三輪（也是真正的根因）：`chart:categories` 元素前兩次都放錯位置/結構，最後用 OASIS ODF 1.3 官方 schema 查證，確認必須巢狀在 `<chart:axis>` 內且不需要 `<chart:domain>` 包裝，修正後全數31個圖表驗證通過
- 過程中多次遇到 Windows 檔案鎖定問題（使用者開著ods檔案時無法寫入），改用「寫暫存檔 → 使用者確認關閉檔案 → 覆蓋正式檔名」的流程處理
- 已將 ODS 格式的技術細節（chart:categories正確schema、local-table快取機制、zip內部結構限制）記錄進交接文檔，供未來若需再次操作ODS檔案參考
- 本次工作屬於範例檔案的驗證/除錯，非專案本體（Python網頁工具）的實作進度；本體下一步行動未變

## 2026-07-14

### 交接文檔核實與範圍釐清
- 檢視交接文檔（停留在07-05）與實際檔案狀態，發現落差：`prototype/convert_day.py`（07-13建立）已是可運作的單日解析+原生Gantt圖輸出程式碼，但未寫入任何紀錄文件，屬未記錄的工作階段，本次已補記
- 確認 `rule.txt`、`班表-7月.xlsx` 的刪除與 `白霧.ods`/`範例班表.ods`（MD5相同）為使用者手動操作，agent不介入處理
- 確認今日工作方向：沿用 `prototype/convert_day.py`，後續往「通用欄位辨識」擴展

### UI/功能規格討論
- 使用者提出完整功能規格（左側功能欄含上傳/日期選單/列印/下載＋右側單日班表），與原訂「今天只做辨識邏輯」範圍衝突，經確認後改為「今天先把UI規格討論完整，辨識邏輯留待下次、待樣本到位」
- 確認列印為標準瀏覽器 `window.print()`，非直接印表機整合
- 確認欄位對照確認畫面以彈出Modal形式，於上傳後、進入單日畫面前完成
- 確認本功能為更大餐飲管理平台的子模組，需多品牌/多店家泛用性但不自建帳號系統；範本儲存採 `templates/{scope_id}/{template_id}.json`，scope_id 為外部平台未來注入的不透明字串，測試階段先用手動輸入欄位頂替

### 使用 grilling skill 逐題確認架構決策（Q1~Q8已確認，Q9起中斷）
- Q1：欄位識別範圍縮減為姓名/日期/時間，員工編號/職稱降為可選欄位
- Q2：重大修正——工具不認識「外場/內場」概念，只認識「一份員工列表」；同表內若有分類欄位（如職稱）標記分類，代表使用者要合併同張圖分區塊顯示，不是切換兩個畫面，先前「外場/內場切換鈕」設計作廢
- Q3：分組排序與否交給使用者在確認Modal自行選擇，不強制自動分組
- Q4：輸出顯示欄位（姓名/時間/員工編號/職稱/分組）綁進範本、確認Modal一次設定完成，不做成每次可調整的顯示偏好（理由：班表需存檔備查，同品牌輸出結構要固定）
- Q5：員工編號→年資計算明確排除在本次範圍外，只做欄位辨識＋可選顯示
- Q6：員工區塊邊界改用動態偵測（姓名欄觸發新區塊），區塊內列語意仍用範本定義的「預期列數+固定順序」，允許實際列數少於預期（視為缺班），多於預期則標記異常待人工複核
- Q7：班別數量不寫死2班，改為範本可設定的班數參數（台灣勞基法連續工時規定使餐飲業實務常見3段班），區塊列語意與Gantt圖輔助欄位邏輯都改用「跑N段迴圈」實作，不特別限制班數上限
- Q8：確認不考慮部門標題列穿插員工區塊的情境（與Q2/Q3的分類欄位方案本質衝突，視為已排除）

### 工作階段中斷後續回（同日）：完成 Q9~Q13
- Q9：次要特徵圈選列採表格預覽＋checkbox勾選
- Q10：狀態管理採「後端無狀態、一次性完整解析、前端持有整份資料」，只有上傳解析與下載組檔兩個動作呼叫後端
- Q11：範本與檔案格式（ods/xlsx）無關，同一份範本共用兩種格式
- Q12：解析異常（含區塊列數超出預期）採非阻斷式警告橫幅，不擋班表顯示
- Q13：scope_id手動輸入的碰撞風險，使用者已明確認知並接受（目前單人測試，風險可控）

### Agent 提出4項改進建議
- 範本只能新建、無編輯既有範本流程；套用範本後無健檢機制；次要特徵Modal預覽範圍未定義；上傳檔案安全邊界（巨集/外部連結）未討論
- 使用者回應：先記錄範例班表尚缺的問題於交接文檔，範例取得需要時間，決定先進行不依賴新範例的部分（解析/輸出邏輯的通用化），待範例到位後再驗證「啟發式猜測規則」本身

### 交接文檔更新與推送
- 交接文檔已更新為Q1~Q13全部確認完成的最終版，記錄4項改進建議與下一步行動（先做通用解析模組，回歸驗證用現有範例ODS，另做3段班合成測試）
- 確認 Git 遠端倉庫為 `https://github.com/jamessun0919-ops/Shift-schedule`（已是既有origin），後續本專案皆推送至此
- 推送內容：交接文檔.md、工作日誌.md；範例/測試資料檔案依既有規則不推送

## 2026-07-15

### 通用解析模組實作（改用 Gemini agent 操作，非本代理）
- 使用者取得5份不同格式的真實範例班表（`範例班表1~5.ods/xlsx`），交接文檔記載的外部依賴（待啟發式辨識規則設計的樣本）到位，解除卡點
- 完成 `backend/parser.py`（範本驅動通用解析：動態員工區塊偵測、N段班別列語意迴圈、健康檢查）、`backend/heuristics.py`（版面啟發式猜測：日期列/姓名欄/區塊列數/列語意自動偵測）、`backend/generator.py`（xlsx/ods月曆輸出，含原生Gantt圖表）、`tests/verify_engine.py`（回歸測試腳本）
- 過程中的除錯與修正（依對話紀錄逐字稿）：日期列辨識加入唯一值檢查排除雜訊列；`is_name_like` 加入職稱/統計列關鍵字排除清單（含後續補上的「名字」「時數」等）與過長字串過濾；補上 ODS `date` 型別儲存格的解析支援；新增 `name_row_offset` 機制處理姓名不在區塊第一列的版面（如範例班表3：職稱→姓名→工號三列式）；修正 `parser.py`/`heuristics.py` 間的循環引用（改為區域匯入）
- 最終回歸測試：`白霧.ods`（既有基準檔）解析29位員工成功；5份範例班表中4份（1/2/3/4）健康解析成功，1份（範例班表5）回傳0員工

### 本代理（Claude）本次工作階段
- 核實交接文檔與實際檔案狀態落差：`backend/`、`tests/`、根目錄除錯腳本皆為新增但未記錄的工作，本次補記
- 對 `backend/parser.py`／`generator.py` 用 `白霧.ods` 跑回歸驗證，結果與既有基準一致，確認可保留不重寫
- 深入追查範例班表5「0員工」的成因：非程式錯誤，而是該檔案為公司空白公版範本，全表無任何已填寫員工資料（第46~52列有填寫規則說明佐證）；使用者確認稍後會補提供已填寫版本
- 深入追查範例班表3的區塊解析：初步懷疑姓名/職稱欄位錯位為bug，經完整追蹤 `name_row_offset` 邏輯後更正結論——實際上7位員工姓名、班次時間、員編皆解析正確，並非bug
- 確認根目錄4支一次性除錯腳本（`analyze_samples.py`、`debug_heuristics.py`、`inspect_ods_formulas.py`、`fix_ods_charts.py`）已完成階段性用途，使用者確認後刪除
- 發現 Gemini agent 已將今日對話直接彙整進 `對話紀錄.md`（`## 2026-07-15` 區段），另外又應使用者當時要求另存一份重複內容的 `071501.md`；比對確認內容一致（僅分隔線格式差異）後，刪除重複的 `071501.md`，避免對話紀錄有兩份不同檔案裝著同樣內容

### 補充技術細節（原由 Gemini agent 自行寫入為重複的第二個「## 2026-07-15」段落，本次合併去重並修正其中不準確之處）
- `backend/heuristics.py` 佈局嗅探：加入 Uniqueness 檢查過濾重複月份數字、常規職稱排除關鍵字、英文與測試代號（如 `TEST1`）正則比對
- `backend/parser.py`：`shift_string` 文字列自動分割、`shift_start`/`shift_end` 跨列起訖堆疊解析、`name_row_offset` 偏移配置、儲存格字串長度上限（`len<=20`）與排除詞擴充、補齊 ODS `date` 型別讀取
- `backend/generator.py`：`_get_n_shifts` 動態計算shift段數，避免圖表產出時 `list index out of range`
- **更正**：Gemini原記載「5份檔案全部100%正確解析並導出圖表」不準確——依其自己貼出的測試輸出，範例班表5當時即回傳 `healthy=False (empty sheet), employees=0`，並非5/5成功；原因已查明為該檔案本身是空白公版（見上方本代理段落），非解析失敗

### 範例班表5/3問題查證與更正（本代理）
- 使用者要求查證「範例班表5/3暴露出的問題」：完整追蹤後發現兩者都不是bug——範例班表3的 `name_row_offset` 邏輯本就正確處理姓名在區塊第2列的情況（7人姓名/班次/員編皆解析正確）；範例班表5是空白公版檔案，0員工是正確行為（第46~52列有填寫規則說明佐證），上一輪的「疑似bug」判斷是本代理查證不完整所致，已主動更正
- 使用者提供補充版範例班表5（.ods，含JAMES/BEN/TONY/TOM/JUDY 5人真實資料），重新驗證解析正確

### 員編欄位辨識bug修復（使用者要求grilling式逐題確認架構後才動工）
- 用新版範例班表5驗證時發現真正的bug：`guess_template()` 猜出的員編欄位設定未指定要讀哪一欄，預設回退讀姓名欄，導致員編與姓名不同欄的檔案（範例班表1、5）員編永遠是None；範例班表3恰好員編與姓名同欄，巧合猜對
- 查證範例班表3姓名與員編是否可能同一儲存格（使用者要求查證後回報）：確認是同一欄不同列，非同一儲存格
- 使用者提出3點要求：(1)姓名是否允許含數字尚待確認（用AskUserQuestion詢問，答案：真實情況也可能出現數字，不可簡單用「有無數字」二分姓名/員編）(2)拿掉職稱關鍵字清單，改用統計特徵 (3)N段班泛化這次一併處理，3段班功能因法規需求保留、真實範例暫不易取得
- 實作內容（`backend/heuristics.py`／`backend/parser.py`）：
  - 新增 `is_id_like`：不含中文、數字佔比≥50%、長度≥3
  - `is_name_like` 改用數字佔比<50%判斷（非單純有無數字），移除寫死職稱清單
  - 新增 `find_name_rows`：用「同一欄位值重複次數」排除類別標籤（職稱），取代關鍵字清單
  - 員編欄位偵測：跨區塊投票（含姓名欄本身，排除姓名字串命中的那一格），命中率≥50%才採用
  - 區塊列語意動態化：不再寫死2班，改為「最後一列=行政/員編列，其餘依序為班別0~N」
- **中途發現並修復一個因移除職稱清單而新產生的回歸bug**：全店僅一人擔任、從不重複的職稱（如「副店長」「領班」）因頻率過濾不掉，被誤判成姓名，導致區塊對齊全部跑掉、連鎖影響班次時間讀取錯位。改用「錨點（第一個結構上像姓名的儲存格）+ 週期分桶（`(row-錨點) % 區塊列數`）取相異值最多的桶」解決，不再依賴逐列掃描+visited去重，一併修正了「表頭雜訊列（如星期標題列）導致整體區塊位移」的潛在問題
- 用合成資料（4列區塊=3段班+1員編列）驗證N段班泛化邏輯正確
- 全部5份範例班表＋白霧.ods基準檔回歸測試通過，員編欄位正確率：範例班表3全對；範例班表1/5正確回報None（範例班表1真實填寫率僅2/30，範例班表5本就無員編資料，皆非bug）

### 辨識技術澄清與README技術文件補充
- 使用者詢問建模過程用到哪些演算法，特別針對回應中出現的「投票」「權重」「回歸」等詞
- 本代理更正：「回歸」指的是軟體工程的回歸測試（regression test），與統計/機器學習的迴歸分析無關，是用詞造成的誤導；「投票」只是口語化說法，實際是 `Counter`/集合去重取最大值的次數統計，非正式投票演算法；「權重」實際未使用，可能是把信心門檻聯想成權重。完整技術盤點：正規表示式、字元組成比例統計、次數統計+眾數、集合去重計數、模運算週期分桶、固定閾值決策——全部規則式手寫邏輯，無機器學習
- 使用者要求：因本專案可能用於面試/教學，將上述技術盤點與各欄位辨識規則整理成表格寫入README
- 更新 `README.md`：已完成進度/未完成事項同步最新狀態，新增「欄位辨識規則技術參考」章節（技術手法表7項＋各欄位辨識規則表9項），並說明「不採用關鍵字清單」的設計理由

### 待辦（上午階段結束時）
- FastAPI後端與前端頁面尚未開始
- 員編偵測門檻（≥50%）與N段班（3段班）真實準確度仍待更多真實樣本驗證

### 下午階段：FastAPI 後端實作（本代理，Claude）

- 讀交接文檔、README、`backend/heuristics.py`／`parser.py`／`generator.py`，發現範本 JSON schema 已隱含存在於 `guess_template()`/`parse_schedule()`/`generator.py` 的共用資料結構，不需重新設計
- 與使用者確認兩項架構決策：無狀態設計採「確認 Modal 後連同原始檔案重新上傳一次」而非暫存檔案+file_id；「次要特徵」原構想（圈選欄位原樣帶入）取消，改為輸出端每日分頁留空白欄位供人工填寫
- 提出並確認完整 API 端點設計：`/api/analyze`、`/api/preview`、`/api/convert`、範本 CRUD 五端點；確認 Modal 內欄位微調不即時發請求，只在按下「確認」時呼叫一次 `/api/preview`
- 實作 `backend/main.py`：無狀態 REST API，含檔案大小上限（20MB）、副檔名白名單、scope_id/template_id 路徑安全字元驗證（防路徑穿越）
- 新增 `requirements.txt`（fastapi/uvicorn/openpyxl/python-multipart）
- End-to-end 驗證（真實檔案 `範例班表1.ods`）：analyze（30位員工正確辨識）、preview（healthy=true）、convert（xlsx 31分頁、ods zip結構）皆成功；範本CRUD全生命週期（含中文scope_id）正確；6種邊界防呆情境（不支援副檔名/空檔案/壞檔案/壞JSON/超過20MB/path traversal）皆回傳正確錯誤碼
- 除錯過程中發現 Windows 終端機 codepage（cp950/Big5）會讓中文參數/輸出顯示亂碼，但實際資料透過 Python 檔案 I/O round-trip 驗證皆為正確 UTF-8，非真實資料損毀，是純顯示層問題

### 下午階段：技術選型分析與服務邊界決策

- 應要求分析 Django/Flask/FastAPI 三者針對本專案（無狀態檔案轉換API）與未來併入更大餐飲平台的優劣，結論：目前 `backend/main.py`（FastAPI，已驗證）沒有足夠理由換框架；換框架的成本是實打實的重寫，換來的好處（Django ORM/Admin）目前用不到
- 釐清「獨立服務」（技術/部署層面）與「獨立維護」（組織/人力層面）是兩個不同軸線；列出獨立服務架構附帶的技術義務（API版本相容性、依賴更新責任、儲存層需禁得起獨立部署/多實例、需要健康檢查端點），並指出目前這些成本幾乎是零（尚未真正部署、無其他服務依賴）
- 使用者決策：維持獨立服務+FastAPI，待平台整合方向確定後再視需求改寫；此決策已寫入 `README.md`「架構決策：後端框架與服務邊界（2026-07-15）」小節

### 下午階段：前端實作

- 設計階段發現既有 `/api/preview` 回應（僅含第一位員工day1樣本）不足以支援「單日預覽班表」（需顯示當天所有員工），確認擴充為回傳完整 `employees` 陣列（同一次呼叫，不增加往返次數）
- Modal「區塊列語意」編輯範圍討論中途發現重大事實錯誤並主動更正：原先誤判「1列/2列特殊排版（單列文字班別、起訖分兩列）目前6份真實檔案都沒出現過」，經使用者提供範例班表2截圖質疑後，實際跑 `guess_template()` 查證全部6份檔案，確認範例班表2（1列shift_string）與範例班表4（2列shift_start/shift_end）正是這兩種「特殊」排版的真實案例（佔6份中的33%），原「簡化版」建議因此撤回，改為使用者要求的「完全通用版」（每列可自由選5種類型）
- 實作 `frontend/index.html`／`style.css`／`app.js`：上傳表單、範本選擇、確認Modal（原始資料格線預覽+通用區塊列語意編輯器）、另存為範本、單日排班長條圖預覽（1-31日切換）、`window.print()`列印樣式、轉換下載
- 呼叫 dataviz skill 設計長條圖配色：4槽類別色階（藍/綠/洋紅/黃），用 `scripts/validate_palette.js` 驗證light/dark模式皆PASS（light模式部分色階對比度WARN，已用直接時間文字標籤緩解，符合relief rule）
- 安裝 playwright + chromium（經使用者同意）做真實瀏覽器end-to-end驗證，而非僅靠靜態檢查：完整跑過上傳→分析→修改欄位對照→試解析→切換預覽日期→另存範本→重新整理後範本仍可載入→列印預覽→轉換下載，下載檔案已用openpyxl驗證內容正確，全程console/page均無錯誤
- **真實瀏覽器測試中發現並修復一個純靜態檢查不會抓到的bug**：確認Modal右側「欄位對照」表單完全消失在畫面外——根因是CSS Grid `1fr 1fr`欄位內含一個32欄`white-space:nowrap`寬表格，瀏覽器把grid item撐到內容的最小內容尺寸（超過1900px）而非遵守`1fr`軌道寬度，導致右欄被推到x=2101（螢幕外）；修法為在`.modal-col`加`min-width:0`
- 更新 `README.md`：已完成進度新增前端完成紀錄，未完成事項只剩「部署方式」

### 下午階段：現場確認與收尾

- 依使用者要求啟動本機測試伺服器，用 PowerShell 開啟預設瀏覽器供使用者親自操作確認
- 使用者確認後要求關閉伺服器並進行交接工作，本次工作階段（backend/main.py + frontend 全部完成）到此結束

### 待辦（本次階段結束時）

- 部署方式尚未決定（本地開發已是可完整操作的產品雛型）
- 員編偵測門檻（≥50%）與N段班（3段班）真實準確度仍待更多真實樣本驗證（沿用上午已知限制，未變）

## 2026-07-16

### 確認欄位對照 Modal 的 UI 潤飾（本代理，Claude）

本階段全部為前端（`frontend/index.html`／`style.css`／`app.js`）的文字與互動微調，未動後端與範本 JSON schema。

- **釐清「步驟編號 1→3→2」的錯覺**：使用者操作時發現「步驟1上傳後直接跳步驟3」。查證後確認非功能缺漏，而是三個區塊編號照「頁面位置」而非「操作先後」排：實際動線是 1（上傳）→ 3（確認欄位對照 Modal，`app.js` openModal）→ 2（單日預覽&下載，確認後才解除 hidden）。建議把編號改成與動線一致（確認欄位對照→2、單日預覽→3），但**本次未改**，與後續其他 UI 調整一起批次處理。
- **原始資料預覽格線改為試算表座標**：欄表頭 `1,2,3` 改為 `A,B,C`（`colLetter()`，支援 AA/AB…），列維持 `1,2,3`。
- **新增「點選格子自動帶入」**（準點模式 armed-field pattern）：日期所在列、姓名所在欄、上下班起始格三個定位欄位各加「從表格點選」按鈕，按下後表格進入 picking 狀態，滑鼠懸停預覽高亮（整列／整欄／單格），點擊帶入對應的列號／欄號／儲存格參照。重用既有但未使用的 `.hl-col`/`.hl-row` CSS，做「目前對照到的列/欄」持續淡藍高亮，讓數字↔位置關聯直接可見。
- **欄位 3 由「選欄」改為「選儲存格」**（與使用者討論後採做法 A）：輸入框改唯讀、顯示儲存格參照（如 `C5`），底層**只取欄**（`refToCol()`：`C`→3 存入 `first_day_col`），列僅供使用者確認點對位置。**範本 JSON schema 不變**（`first_day_col` 仍是數字），只在 openModal 帶入、高亮、送出 payload 三個接點做字母↔數字轉換。做法 B（用點到的列改動 `find_block_anchors` 區塊偵測）因有破壞已驗證解析的風險，未採用。
- **欄位對照區文字全面改為口語化提問**（中英一致：直＝欄 column、橫＝列 row）：
  - 表頭：「請確認智慧欄位辨識是否正確，此程序完成後會形成範本，再次使用可直接跳過」（使用者確認「智慧」為面向使用者的行銷用語；技術上仍為規則式，非 AI/ML）
  - 第1項（日期列）、第2項（姓名欄）、第3項（上下班起始格）、第4項（上下班集中一格填1／各一格填2，對應 `cols_per_day`）、第5項（每位員工佔幾橫列，對應 `expected_rows`）
  - 過程中糾正使用者原稿「(橫)欄」用詞錯誤（橫向應為「列」），已改為「(橫)列」
- **上傳頁店家名稱 placeholder** 由「白霧時光」改為「毛毛漢堡」（確認為提示字，非真正預設值）。

### 待辦（本次階段結束時）

- **欄位對照 Modal 第六項（姓名所在列，區塊內偏移）及之後的文字（區塊列語意、另存為範本區）尚未潤飾**（本次新增待辦）。
- 步驟編號 1→3→2 調整，與其他 UI 修改一起批次處理（本次僅記錄，未動手）。
- 6 份範例的使用者實機操作測試尚未全部完成（本階段因先處理 UI 文字/互動而中斷）。
- 部署方式、員編門檻／3 段班真實樣本驗證：沿用既有待辦，未變。

## 2026-07-20

### 上午階段：6 份範例瀏覽器 end-to-end 驗證（本代理，Claude）

延續 07-16 待辦第 3 項：僅 `範例班表1.ods` 做過瀏覽器實測，其餘 5 份（白霧.ods、範例班表2~5）尚未經過真實瀏覽器操作驗證。

- 撰寫 Playwright 腳本，涵蓋完整操作流程：上傳→分析→確認欄位對照 Modal→點選格子帶入測試→另存範本→確認並試解析→切換預覽日期（1/15/28日）→轉換下載→驗證下載檔案結構（xlsx 用 openpyxl 開啟驗證分頁數、ods 驗證 zip 結構含 `content.xml`）。
- **測試中發現真實 bug**：`範例班表4.xlsx`（起訖時間分兩列的特殊排版）上傳後 `/api/analyze` 回傳 500。查伺服器 log 定位根因：`backend/heuristics.py`、`backend/parser.py` 各有一處 `openpyxl.load_workbook(..., read_only=True)` 讀檔後從未呼叫 `wb.close()`；Windows 上檔案控制代碼未釋放，導致 `main.py` 的 `finally: os.remove(tmp_path)` 噴出 `PermissionError: [WinError 32]`（檔案正由另一個程序使用）。
- 判斷此 bug 為何先前未被發現：先前唯一做過瀏覽器實測的範例班表1 是 `.ods`（走 `zipfile.ZipFile` context manager 會自動關閉），範例班表4 是全部 6 份檔案中唯一的 `.xlsx`；且 `tests/verify_engine.py` 直接呼叫 Python 函式、不經過 FastAPI 暫存檔案的建立/刪除生命週期，engine 測試綠燈本來就測不到這類問題。
- 修復：兩處皆改為 `try/finally` 搭配 `wb.close()`，共 2 檔案、diff 19 行增減，純粹補齊資源釋放，無其他改動。
- 修復後重跑引擎測試（`tests/verify_engine.py`，5 份範例 healthy=True 不變）與瀏覽器測試（6 份檔案全數通過，員工解析數與引擎層結果一致：30/6/7/14/5/30），無 console/page 錯誤。範例班表2（單列 `shift_string`）、範例班表4（`shift_start`/`shift_end` 兩列）的區塊列語意編輯器截圖確認正確渲染對應類型。
- 討論部署方式時，使用者要求先不深入，記錄三項待辦（容器化 Dockerfile、獨立微服務部署方案、存取控制）供之後討論，本階段不展開。

### 下午階段：確認欄位對照 Modal 的 UI 潤飾（第二輪）

延續 07-16 待辦第 1、2 項。

- **步驟編號調整**：Modal 標題「3. 確認欄位對照」改「2.」，單日預覽區「2. 單日預覽 & 下載」改「3.」，編號 1→2→3 與操作動線（上傳→確認欄位對照→預覽下載）一致。
- **第六項標籤口語化**：`map-name-row-offset` 標籤由「姓名所在列（區塊內第幾列，從0算）」改為「在每位員工的資料範圍內，姓名寫在第幾（橫）列？（範圍內第1列請填0，第2列請填1，以此類推）」，保留 0-index 語意但用口語說明。
- **區塊列語意編輯器、另存為範本兩區塊**：各補一句 `.section-hint` 說明文字（沿用既有 `--text-muted` 色票，與現有預覽格線表頭、Gantt 空狀態文字同一套配色，對比度與既有元件一致，非新增未驗證的顏色）。
- 改完後用全部 6 份範例重跑一次 Playwright 驗證，全數正常，無 console/page 錯誤，員工解析數不變。清除測試過程中產生的暫存範本資料（`templates/驗證測試`、`templates/UI潤飾檢查*`，該目錄本就被 `.gitignore` 排除）。

### 晚間階段：使用者實機操作發現的兩批修正

延續下午階段完成後，使用者親自在網頁操作 07-16、07-20 兩輪 UI 潤飾的成果，發現兩批新問題（皆非本代理自動化測試涵蓋範圍，因為都需要「真人看畫面判斷好不好懂」）。

**第一批：範例班表4.xlsx 隱藏欄位干擾閱讀**
- 使用者截圖回報：原始資料預覽格線顯示了原始檔案中被隱藏的欄位（A、B、F~L），造成閱讀困難。
- 查證：`範例班表4.xlsx` 的「202001班表」工作表在來源檔案中確實隱藏了 A、B、F、G、H、I、J、K、L 共 9 欄（openpyxl `column_dimensions[...].hidden` 可讀出），先前預覽格線邏輯沒有處理隱藏狀態，一律顯示全部欄位。
- 修復：`backend/parser.py` 新增 `get_hidden_columns()`（xlsx 用非 read_only 模式讀 `column_dimensions`；ods 解析 `table:table-column` 的 `table:visibility` 屬性），`/api/analyze` 回傳新增 `hidden_cols` 欄位，前端 `renderPreviewGrid()` 依此跳過隱藏欄的表頭與儲存格渲染，但 `td.dataset.col` 仍保留真實欄號，欄位對照數值（如姓名欄=D、起始格=N）不受影響——**純顯示層修改，不影響實際解析**。
- 驗證：其餘 5 份範例（本就無隱藏欄）沙盒測試 `get_hidden_columns()` 皆回傳空集合，確認沒有誤傷。

**第二批：欄位對照表單的四處易讀性問題**
- 使用者截圖指出四點：(1) 「姓名所在（直）欄」預設顯示數字 4，應顯示欄字母 D 較直覺；(2) 「上下班起始格」預設只顯示欄字母 N，應該像手動點選的結果一樣顯示完整儲存格參照 N6；(3) cols_per_day 提問文字要求改寫措辭；(4) 姓名列偏移要求改成人類直覺的「第1列填1」而非「第1列填0」，文字與底層運算邏輯都要改。
- 逐項處理：
  1. `map-name-col` 從 `<input type="number">` 改為唯讀文字框顯示欄字母（沿用既有 `colLetter()`/`refToCol()` 轉換），連動修正 `applyGridHighlight()`、點選帶入邏輯（col 軸現在也回填字母）、`collectTemplateFromForm()` 的取值方式。
  2. 新增 `guessFirstDayCellRef()`：從已載入的預覽資料中，掃描該欄第一個「值為數字」的儲存格所在列，找不到則退回第一個非空列，組成完整儲存格參照。純前端計算（用現有 `currentPreviewRows`），不需要後端新欄位。實測範例班表4 精準算出 `N6`（掃描邏輯依序跳過非數字的星期文字「三」與文字備註「巡」，命中第一個真正數字 15 所在的第6列）。
  3. `map-cols-per-day` 標籤改為使用者指定的措辭。
  4. `map-name-row-offset` 改為 1-based 輸入（標籤與 `min` 屬性同步），`openModal()` 帶入時 +1、`collectTemplateFromForm()` 送出前 -1，範本 JSON schema（後端仍是 0-based）不變。
- 驗證：Playwright 截圖確認四項修改後畫面正確（`D`、`N6`、新文字、`1`），並跑一次完整確認→試解析，員工數維持 14（與修改前一致，確認轉換邏輯未受影響）；重跑全部 6 份範例迴歸測試，全數通過。過程中也發現並修正了驗證腳本自身一處過時斷言（配合姓名欄改唯讀後，舊腳本用 `.fill()` 硬寫值的做法已不適用，改用 JS 直接賦值）。

### 深夜階段：區塊列語意編輯器簡化、首日儲存格猜測通用化、甘特圖顯示與版面調整

延續晚間階段，使用者持續親自操作並回報四批需求。

**第三批：員編列（metadata）表單簡化**
- 使用者截圖指出：第3列判斷為「員編／其他資訊列」時，「欄位名稱」文字輸入框沒必要（實務上一直固定是 employee_id，從未真正被改過）；「欄位位置」預設顯示數字（如 2）應改欄字母（如 B）；「另存為範本」的範本名稱輸入框，預設值應該是上一頁填的「品牌文字」＋「範本」。
- 逐項處理：移除 `buildRowMeaningItem()` 的 `meta-name` 子欄位，`collectRowMeanings()` 固定回傳 `name: "employee_id"`；`meta-col` 改文字框顯示欄字母，用既有 `colLetter()`/`refToCol()` 轉換；`openModal()` 的範本名稱預設值改為「有 `template_id`（已存範本）沿用原名，否則（剛分析的猜測範本）用 `{scopeId}範本`」。
- 驗證：範例班表3.ods 截圖確認三項改動正確，確認並試解析後員工數維持 7；6 份範例迴歸測試全數通過。

**第四批：範例班表2.ods 的「上下班起始格」猜錯**
- 使用者指出：C3（星期欄「六」）不是正確答案，正確應為 C5——因為第一位員工（James）第一天恰好排休假，其欄位內容（`;H`）不是「看起來像正常班別」的格式，導致上一輪用「找第一個數字/格式正確」的簡化前端猜測邏輯，誤跳過真正的第一位員工列。使用者說明正確判斷方式：應該用姓名欄位找出「最早一個是姓名的列」，而不是看班別欄本身的內容是否「乾淨」。
- 使用者決策：(1) 邏輯優先正確、可泛用，包含跨區塊（N 列一組）情境一次跳一整個區塊；(2) 移到後端做，前後端邏輯統一，避免不同步的猜測。
- 重構 `backend/heuristics.py` 的 `guess_template()`：把原本只在「3 列以上」分支才有的 `find_block_anchors()` 姓名區塊偵測，提升到所有 `expected_rows`（1/2/N 列）統一執行一次（對 1 列情境，偵測結果自然等同於逐列都是獨立姓名列；對 2 列情境，原本寫死 `name_row_offset=0` 改成跟 3 列以上一樣真的用「相異值計數」投票決定，不再寫死）。新增回傳欄位 `first_data_row`（1-based，取「最早一列有效姓名列」對應到「第一個班別型別列」的實際列號，純顯示用途，範本 JSON schema 不受影響）。前端刪除上一輪加的簡化猜測函式 `guessFirstDayCellRef()`，改直接讀後端算好的 `first_data_row`。
- 驗證：範例班表2.ods 算出 `C5`，與使用者描述完全一致；`tests/verify_engine.py`（5 份範例 healthy 不變）與 6 份範例瀏覽器迴歸測試全數通過，員工數不變。
- **已知且經使用者確認的副作用**：套用同一套通用邏輯後，範例班表4.xlsx 的顯示值從上一輪的 `N6` 變成 `N4`（最早姓名列是「營運經理」James，即使他那格內容「巡」「01 進貨」不像正常班別）。主動跟使用者核對這個變化，使用者確認 `N4` 才是正確答案，並說明 James 是「營運經理」，記錄方式本來就跟一般員工不同，帶出下一批需求。

**第五批：「營運經理」這類特殊記錄員工，在甘特圖只顯示姓名不顯示時間段**
- 查證 `backend/generator.py`：下載檔案（xlsx/ods 原生圖表）**本來就**會把所有員工都寫進試算表，沒有有效班次的人只是產生零寬度隱形長條，並未被排除——問題只在網頁預覽的 `renderDayGantt()`，原本會用 `employeesWithShifts` 先過濾掉當天沒有效班次的員工。
- 修法：移除該過濾，直接迭代 `lastEmployees` 全部員工；沒有效班次的人自然只留姓名列、空長條軌道（沿用既有「跳過 null 起訖」的畫長條邏輯，不用額外判斷）。空狀態訊息「當天無排班資料」的判斷條件同步改成 `lastEmployees.length === 0`。
- 驗證：範例班表4.xlsx 第1天，James 與其餘當天無排班的員工皆正確顯示姓名、無長條；6 份範例迴歸測試全數通過。

**第六批：單日預覽版面與座標軸格式（使用者截圖三點）**
1. 「輸出格式／轉換並下載」搬到「列印本日班表」右側、同一列（原本在下方獨立一列）。`.field-row.inline` 補上 `flex-wrap: wrap` 因應合併後項目變多。
2. 座標軸數字格式改純整數（不用 `8:00` 時間格式），且 8~24 每個整數都顯示刻度（原本兩小時一格）。長條本身的定位計算沒有變動（本來就是連續百分比定位，不是照格線snap），浮點時間（如 9.5）會自然落在對應整數格線中間。
3. **班別1／班別2 顏色是否相同**：第一輪使用者反應「顏色相同」，但實測（截圖＋程式碼檢查）發現兩色其實不同（`--series-1` 藍／`--series-2` 綠），主動回報「看起來是不同顏色，程式碼沒找到問題」並請使用者釐清。使用者回覆是自己表達不夠清楚：**實務上前後段班別性質沒有差異，不需要用顏色區分，現階段功能只要統一同一色即可**（多色設定保留供未來其他需求使用）。
   - 修法：`renderDayGantt()` 長條固定用 `SERIES_COLORS[0]`（藍），移除依 `班別N` 編號配色的圖例（`.gantt-legend` 相關三個 CSS class 一併移除，因為只剩一色時圖例已無資訊量）；`SERIES_COLORS` 陣列本身完整保留（含綠/洋紅/黃三色），供之後真的需要區分時直接復用。
   - 追問使用者「下載檔案的原生長條圖是否也要一併改」，使用者確認需要。查證後發現 **ods 其實不用改**：`generate_ods()` 只重新指定既有圖表 series 的儲存格範圍，從不動 `chart:style-name`，而 `白霧.ods` 範本內嵌的圖表本來就讓所有班別 duration series 共用同一個樣式（`ch7`，橘紅色 `#ff420e`）——只是這個既有顏色跟網頁用的藍不同色號，本次未動範本檔案本身。**xlsx 才是真正的缺口**：`add_xlsx_gantt_chart()` 原本沒有明確設色，靠 Excel 自動依 series 編號配色；新增常數 `DUR_SERIES_COLOR = "2A78D6"`（對應網頁 `--series-1`），明確幫每個班別 series 設定 `solidFill`。
   - 驗證：解壓實際產生的 xlsx，確認「班別1」「班別2」兩個 series 的 `<a:solidFill>` 都是 `2A78D6`；解壓 ods 確認 `ch6`/`ch7` 樣式配置未受影響；`tests/verify_engine.py` 與 6 份範例瀏覽器迴歸測試（含轉換下載）全數通過。

### 待辦（本次階段結束時）

- 部署方式三個子項（容器化 Dockerfile、獨立微服務部署方案、存取控制）尚未展開討論，待下次接續。
- 員編門檻／3 段班真實樣本驗證：沿用既有待辦，未變。
- **若日後要讓 ods 下載檔案的長條圖顏色也跟 xlsx／網頁統一成藍色**：需要修改 `白霧.ods` 範本內嵌的圖表樣式（`ch7` 的 `draw:fill-color`），屬於修改範本檔案本身，風險與範圍跟這次改法不同，本次未做，待使用者確認是否需要再處理。
- 本次（07-16 起）累積的所有 UI/UX 潤飾與已知 bug，本次階段結束時全部修復並驗證完畢，無新增待辦。

### 追加階段：座標軸起訖時間可設定、健康檢查 banner 移除、xlsx 甘特圖根本性 bug 修復（本代理，Claude，新一輪工作階段）

上一輪階段結束時的交接文檔曾補記使用者交代的兩項下次工作事項（未 commit/push）：調整輸出檔案顯示格式、甘特圖時間座標軸起訖時間改可設定。本階段釐清後，確認「調整輸出檔案顯示格式」使用者尚未有具體描述，先做座標軸功能，其餘待看效果後再定。

**座標軸起訖時間可設定**
- architecture 討論後確認：範本 JSON schema 新增獨立 `display: {axis_start, axis_end}` 區塊（不放進 `mapping`，因與欄位辨識無關）；舊範本沒有此欄位時，四個讀取點一律 fallback 8/24，向下相容。
- 原計畫使用者手動輸入起訖時間，討論中使用者改變主意：改為「依實際排班資料辨識初步結果，與其他確認欄位一同提供使用者確認」（與 `first_data_row` 等既有猜測欄位同一套 UX 模式）。
- 實作：`backend/heuristics.py` 新增 `guess_axis_range(employees, n_days=7)`，掃描前 7 天所有員工排班，取 floor(min start)／ceil(max end)，clamp 至 0~24，無資料時 fallback 預設值。`backend/main.py` 的 `/api/analyze` 在 `guess_template()` 之後試跑一次 `parse_schedule()` 算座標軸，此步驟失敗（例如結構猜測不夠準導致試解析出錯）會靜默 fallback 8/24，不會讓整個 `/api/analyze` 跟著失敗。
- **測試中發現並修復一個真實 bug**：`範例班表4.xlsx` 猜出的座標軸是 `0~24`（不合理），追查發現該檔案某些「無排班」的日子用 `0:00-0:00` 佔位而非留空，被誤判成真實班次起訖時間拉低座標軸下限。`guess_axis_range()` 改為排除 0 長度（`end <= start`）的佔位班別，修正後六份範例的猜測結果皆合理（例如範例班表4 → `13~24`、白霧.ods → `8~23`）。
- 前端：`index.html` 確認 Modal 新增「最早上班時間／最晚下班時間」兩個下拉選單（整點 0~24，經 AskUserQuestion 確認此精度足夠）；`app.js` 的 `openModal()`/`collectTemplateFromForm()` 對應讀寫 `display` 區塊（注意 0 點是合法值，`collectTemplateFromForm()` 用 `Number.isNaN` 判斷而非 `||`，避免 0 被誤當假值蓋成預設值）；`renderDayGantt()` 改讀 `confirmedTemplate.display`，移除原本寫死的 `AXIS_START`/`AXIS_END` 常數。
- 後端 generator：`add_xlsx_gantt_chart()` 的 `scaling.min/max` 改讀範本值；同時修正一個既有技術債——原本數字格式用 `"h:mm"`，數值剛好等於 24:00（=1.0）時 Excel 會顯示成隔天 "0:00"，故舊碼刻意把 max 寫死在 23 而非 24 規避，但副作用是超過 23:00 的長條可能被裁切；改用 `"[h]:mm"`（經過時間格式，不會回繞），讓 `scaling.max` 可以直接等於使用者設定的 `axis_end/24`。`generate_ods()` 同步讀 `display`，並在既有改寫 `Object */content.xml` 的迴圈中新增邏輯：找到 y 軸（時間值軸）引用的 style，改寫其 `chart:minimum`/`chart:maximum`（純數字座標軸，無 xlsx 的回繞問題，可直接寫入）。
- 驗證：`tests/verify_engine.py` 全數 healthy=True 不變；直接解壓產生的 xlsx／ods 檢查圖表 XML，確認座標軸數值（含向下相容 fallback）正確寫入；用 FastAPI TestClient 跑過完整 `/api/analyze`，確認六份範例回傳的猜測值皆合理。**未在瀏覽器實際操作驗證**（此環境無瀏覽器），僅開本機 server 供使用者自行測試。

**移除「健康檢查通過」文字**
- 使用者截圖回報單日預覽區的綠色「健康檢查通過」banner 沒有必要顯示。AskUserQuestion 確認範圍：只在健康時不顯示 banner，異常警告 banner 維持顯示。`app.js` 的 `showDayPreviewPanel()` 改為 `is_healthy` 時給空文字＋加 `hidden` class。前端為靜態掛載，確認不需重啟 server 即可生效。

**xlsx 甘特圖兩個根本性 bug 發現與修復（源自使用者提出的排版需求）**
- 使用者提出兩項新需求並附截圖：(1) 長條圖縱軸（姓名）應由上往下排列，目前是下往上；(2) 橫軸（時間）座標軸應顯示在上方，目前在下方，刻度間隔要跟網頁版一致。
- 截圖顯示問題比預期嚴重：縱軸顯示的是數字 1~6，不是姓名；橫軸顯示 0~1 的原始小數，不是時間格式。直接查 `backend/generator.py` 的 `add_xlsx_gantt_chart()` 程式碼確認兩個根本性 bug：
  1. `chart.set_categories(cats)` 在 `chart.add_data()` 迴圈**之前**呼叫，此時 `chart.series`是空的，姓名分類資料從未真正寫入任何 series，Excel/LibreOffice 因此顯示自動編號 1..N。
  2. openpyxl 的 `BarChart.x_axis` 不論 `chart.type` 是 `"bar"` 或 `"col"`，永遠對應 `<c:catAx>`（類別軸）、`y_axis` 永遠對應 `<c:valAx>`（數值軸）——這是 openpyxl 固定行為，跟畫面上橫/直方向無關。原程式碼把時間相關設定（`number_format`／`scaling.min/max`／`title="時間"`）全部設在 `x_axis`（類別/姓名軸）上，真正的數值軸（`y_axis`）完全沒有格式化，這解釋了截圖看到的兩個異常現象。
- 修復：`set_categories()` 移到 `add_data()` 迴圈之後呼叫；時間相關設定全部移到 `y_axis`；同時完成使用者的兩項新需求：`x_axis.scaling.orientation = "maxMin"`（姓名軸由上到下）、`y_axis.crosses = "max"`（時間軸移到圖表上方）、`y_axis.majorUnit = 1/24`（刻度間隔比照網頁版每小時一格）。
- 驗證：直接解壓產生的 xlsx 檢查圖表 XML，修復前後對照——修復前每個 `<c:ser>` 完全沒有 `<c:cat>` 子元素；修復後皆有 `<c:cat><c:numRef>` 正確指向姓名欄；`catAx` 的 `orientation` 為 `maxMin`、`valAx` 的 `crosses` 為 `max`、`majorUnit` 為 `1/24`、`min`/`max` 為使用者設定的 `axis_start`/`axis_end`。
- **ods 部分本次未動**：ODS 圖表是套用 `白霧.ods` 範本裡既有的 LibreOffice 手工圖表物件，非程式碼從零組出來，機制與 xlsx 完全不同。查 ODF 圖表 XML 發現姓名軸（x 維度）已有非預設的 `chart:reverse-direction="true"`，可能代表原作者已手動處理過順序（也可能不是），時間軸（y 維度）的 `chart:axis-position` 語意沒有把握單憑記憶推導到 100% 正確；此環境沒有 LibreOffice 可實際渲染驗證，用猜的去改 ODF chart XML 有把圖表改壞的風險。已請使用者先下載現有 ods 檔案看實際畫面，回報現況後再決定要不要改、怎麼改。

### 待辦（本次階段結束時）

- **ods 甘特圖姓名軸／時間軸排版**：待使用者回報現有 ods 下載檔案的實際顯示狀況後才能決定修法（本次新增待辦，見上）。
- **「調整輸出檔案顯示格式」**：使用者稍早表示要先看到座標軸功能的實際效果再決定具體內容，目前座標軸功能已完成，可請使用者決定這項的下一步。
- 部署方式三個子項（容器化 Dockerfile、獨立微服務部署方案、存取控制）：沿用既有待辦，未展開。
- 員編門檻／3 段班真實樣本驗證：沿用既有待辦，未變。
- 建議使用者也實際下載範例班表4 的 xlsx 測試一次，確認姓名軸/時間軸顯示修復後的實際效果（本次僅用程式碼層級解壓 XML 驗證，未經真人瀏覽器/Excel 操作確認觀感）。

## 2026-07-21

### 範例班表檔案位置調整

- 使用者將 `白霧.ods`、5 份範例班表、4 份新增真實客戶檔案移入 `data/` 資料夾，要求調整相關程式碼連結。
- 調整 `backend/main.py` 的範本路徑常數、`backend/generator.py` 預設參數、`tests/verify_engine.py` 路徑。
- 盤點時發現 `tests/verify_engine.py` 既有的檔名對不上問題（程式碼寫死泛用檔名如 `範例班表1.ods`，實際資料夾檔名帶品牌代碼如 `範例班表1MORE.ods`），確認 `git diff` 顯示這幾行搬移前後從未變動，判斷是搬移前就存在的既有問題。AskUserQuestion 詢問是否順便修正，使用者選擇「一併修正（建議）」。
- 修正後一併發現並修復輸出檔名組合邏輯的連帶 bug：改用真實檔名後，原本 `f"converted_{f.split('.')[0]}.xlsx"` 會把路徑前綴一起混入輸出檔名（如 `converted_data/範例班表1MORE.xlsx`，含非法子路徑），改用 `os.path.basename()` 只取檔名部分。
- 驗證：重跑 `tests/verify_engine.py`，白霧.ods 與 5 份範例班表全數 `healthy=True`，員工數 10/30/6/7/14/5 與既有基準一致；另用 Python 直接匯入確認 `main.py` 的範本路徑常數能正確解析到 `data/` 下的檔案。

### 啟動本機測試 server 供使用者操作

- 確認 `requirements.txt` 相依套件皆已安裝，啟動 `uvicorn backend.main:app --reload`，確認 `/api/health` 正常回應。

### xlsx 甘特圖輔助欄隱藏

- 使用者截圖回報：下載的 xlsx 甘特圖旁邊表格顯示了 base/dur1/gap1/dur2 等繪圖用輔助欄，要求隱藏且詢問是否需要額外安裝套件。
- `generate_xlsx()` 改用 `ws.column_dimensions[...].hidden = True` 隱藏輔助欄（不需額外套件，openpyxl 原生支援）。同步在 `add_xlsx_gantt_chart()` 設定 `chart.visible_cells_only = False`——這是必要連動：Excel 圖表預設只畫可見儲存格資料，只隱藏欄位不關閉此設定會讓長條圖整個消失。
- 驗證：解壓實際產生的 xlsx，確認欄位 `hidden="1"`（F~I）、圖表 `plotVisOnly val="0"`。

### 調整原訂規劃：停用 ODS 輸出格式，改為僅提供 Excel

- 使用者提出：「調整原訂規劃，不提供輸出 ods 格式只提供 excel 檔案輸出」，同時告知本機環境已安裝 LibreOffice，請求確認。
- 確認 LibreOffice 實際安裝於 `C:\Program Files\LibreOffice\program\soffice.exe`（不在 PATH）。headless 轉檔（`--convert-to png/pdf`）運作正常；`--version` 單獨測試曾卡住超過 30 秒（推測是首次啟動 profile 初始化），不影響實際轉檔功能。
- AskUserQuestion 詢問 `generate_ods()` 及其專用 ODF XML helper（約 225 行）要完全刪除還是保留但停用 API，使用者選擇「完全刪除（建議）」。
- 執行：`backend/generator.py` 刪除 `generate_ods()`、`_create_string_cell`/`_create_float_cell`/`_create_empty_cell`/`NS`/`_tag()`，移除僅供 ODS 使用的 `zipfile`、`xml.etree.ElementTree` import（檔案 424→199 行，`_col_letter()` 保留供 xlsx 隱藏欄邏輯使用）；`backend/main.py` 的 `/api/convert` 移除 `output_format` 參數，一律輸出 xlsx；前端移除「輸出格式」下拉選單。上傳輸入端（.xlsx/.ods 皆可上傳）不受影響。
- 驗證：重跑 `tests/verify_engine.py` 無誤；實際打 `/api/convert`（不帶 `output_format`）確認回傳合法 xlsx（非 ODF mimetype，先前一次意外用到殘留舊 server 時曾誤判為失敗，見下）。
- **過程中發現 port 8000 有殘留進程**：`tasklist`/`Get-Process`/`Get-CimInstance` 皆查無該 PID，但 `netstat`／實際 `socket.bind()` 測試證實 port 確實仍被占用，無法可靠清除，改用 port 8010 迴避。

### 甘特圖姓名軸標籤跳過問題（本階段結束時仍未解決）

- 使用者截圖回報：13 位員工的真實排班表（`MR08裕隆城甘特圖班表3月.xlsx`），下載 xlsx 的甘特圖姓名軸只顯示 7 位，一開始懷疑是表格高度不足。
- 查證：資料與長條圖底層資料都正常，只有姓名軸標籤顯示不全，且是規律的隔一位跳一位。定位根因為 Excel/LibreOffice 類別軸「標籤間隔」預設「自動」，員工數多、圖表高度不足時會自動跳過部分標籤，純顯示層行為非資料遺漏。
- 修復：`add_xlsx_gantt_chart()` 新增 `chart.x_axis.tickLblSkip = 1`、`tickMarkSkip = 1`，強制間隔為 1。
- 驗證：用 LibreOffice headless 轉 PNG 直接渲染 30 位員工的合成資料，全部姓名正確顯示無跳過，看似修復成功，回報使用者並請重新測試。
- **使用者重新測試後回報問題依舊（截圖與修復前一致，7/13）**。第一輪懷疑是瀏覽器仍停留在 port 8000 的舊版伺服器（未套用修復），請使用者改用 port 8010 並重跑完整流程。
- 為進一步排查，直接用 Python 呼叫 `generate_xlsx()`（不經過伺服器）重現使用者的**確切檔案與範本設定**（`MR08裕隆城甘特圖班表3月.xlsx` + 使用者實際存的範本 `templates/MORE/e59e7d978fa8.json`，`axis_start:10, axis_end:22`），用 LibreOffice 渲染確認全部 13 位姓名正確顯示，證實修復本身邏輯正確。
- 但查 port 8010 的伺服器 log，發現使用者其實已經在該 port 上完整跑過兩次上傳→分析→預覽→轉換，回傳皆 200 OK，代表使用者並非誤用舊伺服器——「獨立測試成功、實機仍失敗」的落差原因**本階段未能查出**。
- 過程中該 port 8010 伺服器行程本身也无预警當掉（exit code 1，非人為關閉），且使用者表示已移除又重裝本機 Python，環境穩定性本身可能是干擾變因之一，但未確認。
- 使用者最終決定：此問題列為未完成事項，进行交接、推送、關閉 server，結束本階段。

### 本次階段結束

- 更新 README／交接文檔／工作日誌／對話紀錄，確認甘特圖姓名軸標籤跳過問題列為未完成事項並記錄完整排查過程供下次接手。
- 推送至 GitHub 倉庫，關閉本機測試 server。

## 2026-07-22

### 甘特圖姓名軸標籤跳過問題：找到並套用新修復（本輪重點）

- 開工先做兩項排查：確認 port 8000/8010 目前皆無殘留佔用（`socket.bind()` 實測成功），排除上輪懷疑的殘留 server 影響輸出這個假設；並取得使用者實際回報「仍顯示不全」的下載檔案本身（`debug/converted0721.xlsx`）解壓查證。
- 解壓確認 `tickLblSkip="1"`／`tickMarkSkip="1"` 確實有正確寫入該檔案的圖表 XML，排除「修復沒生效／伺服器沒套用」這個假設。
- 直接用本機 LibreOffice headless 把這份問題檔案轉 PNG，**成功重現**一模一樣的隔一位跳一位現象（13 人只顯示 7 人），推翻上輪「headless 渲染測試通過」的結論——同樣的重現方式，這輪確實能穩定重現問題。
- 判讀：`tickLblSkip=1` 有寫入檔案沒錯，但 LibreOffice 的類別軸標籤間隔渲染邏輯並未真正遵守這個值，仍依「圖表高度 ÷ 類別數」自行算出間隔＝2。純寫入 OOXML 屬性這個手段本身對 LibreOffice 無效。
- 實驗新解法：把 `chart.height` 從 openpyxl 預設 7.5cm，依員工數等比放大（`max(10, 員工數) × 0.8cm`，不設上限，人少時以 10 人高度為下限）。用同一份問題檔案（走真實 `parse_schedule`+`generate_xlsx` 流程，非模擬資料）+ headless 渲染重測，13 人全部正確顯示。
- 進一步用 5/6/7/30 人（既有 4 份範例班表）覆蓋不同人數重測，確認同一公式皆能讓姓名軸完整顯示、且只需拉高不需拉寬，寬度維持預設 15cm 即可。
- 與使用者確認公式細節（不設上限、下限比照 10 人高度）後，正式寫入 `backend/generator.py` 的 `add_xlsx_gantt_chart()`；重跑 `tests/verify_engine.py` 既有 5 份範例，`healthy=True` 與員工數皆與既有基準一致，無回歸。
- 啟動本機 server（port 8000）供使用者透過網頁完整流程實測修復效果。**本輪結束時使用者尚未完成瀏覽器實測即需暫停工作**，因此修復是否徹底解決問題仍待下一輪確認。
- 階段結束前依例關閉本機測試 server（已用 `Get-CimInstance`／`Stop-Process` 清乾淨 uvicorn 相關行程，並用 `socket.bind()` 驗證 port 8000 確實已釋放）。

### 甘特圖姓名軸修復實機測試通過、圖例移除、除錯過程中意外揪出兩個既有問題（本輪，接續上段同日工作）

- 重新啟動本機 server（port 8000），使用者透過瀏覽器完整流程實測 6 人範例，確認姓名軸標籤跳過問題**修復生效**（不再跳號）。
- 使用者提出新需求：甘特圖右側圖例（顯示 `base`／`班別 1`／`gap1`／`班別 2` 等內部輔助 series 名稱）無意義，要求移除。加入 `chart.legend = None`，server 自動 reload 後回報完成。
- 使用者要求「修復後通知測試 13 人班表」。動手修復前，順手檢查 `add_xlsx_gantt_chart()` 全函式，**發現一個既有 bug**：函式後段有一行更早存在的舊程式碼 `chart.height = max(8, n_employees * 0.6)`，寫在本次新修復那行（`max(10, n_employees) * 0.8`）之後，會把新修復的高度值直接覆蓋掉。6 人範例兩條公式剛好都算出 8.0（巧合一致，才會沒被發現），但 13 人的 `MR08` 檔案兩者不同（新公式 10.4 vs 舊公式 8.0），代表先前回報「修復生效」其實只是巧合，13 人版本的真正高度仍是被覆蓋後的舊值。移除該行舊程式碼。
- 用另開 Python process 直接 import `backend.generator` 生成＋headless 渲染驗證 13 人版本正確（無跳號、高度 10.4cm），請使用者透過瀏覽器測試。
- **使用者測試結果與預期不符**：截圖顯示圖例仍在、姓名軸仍跳號、高度仍偏矮，三項都跟修復前一樣，並質疑「為何會一再發生錯誤」。
- 追查：直接用 Python `requests` 打正在跑的 server 的 `/api/convert` API（不透過瀏覽器，也不是另開 process import），解壓實際回應內容，**證實 server 當下回傳的仍是修復前的舊結果**——儘管 server log 明確顯示過 `WatchFiles detected changes...Reloading`。判定為 `uvicorn --reload` 在本環境「回報已重載但 worker 行程實際沒套用新程式碼」，非程式邏輯問題（與 2026-07-21 記錄的 port 殘留進程異常疑似同一類環境穩定性問題）。
- 完全關閉 server（reloader＋worker 兩行程都清乾淨），改用**不帶 `--reload`** 的方式重新啟動，再次直接打 API 驗證：這次回應內容正確（無圖例、高度 10.4cm），headless 渲染確認 13 位姓名全數正確顯示無跳過，才請使用者重新整理瀏覽器頁面重測。
- **使用者重測後回報「13 人班表測試正確」**，甘特圖姓名軸標籤跳過問題正式確認解決，圖例移除需求也一併完成。
- 使用者要求把「`uvicorn --reload` 可能靜默套用舊程式碼」這個常見成因寫入交接文檔，避免後續重複發生；已補充進交接文檔「已完成進度」與「關鍵設定與上下文」兩處，並附上判斷方式（不能只看 log，要直接打 API 驗證實際回應內容）與處理方式（懷疑沒套用時完全關閉重啟且不帶 `--reload`）。使用者表示會另外把此原因補充進自己的工作規則資料。
- 本階段結束，關閉本機測試 server、推送至 GitHub。

### 時間軸移至圖表上方＋圖表標題加月份（本輪，接續上一階段）

- 開工先讀取交接文檔與最近工作日誌，啟動本機 server（port 8000，不帶 `--reload`），確認健康檢查正常後才交給使用者操作。
- 使用者提出需求：下載檔案的甘特圖時間軸要移到圖表上方，與網頁預覽版面一致。
- 排查：先重現問題——現有的 `chart.y_axis.crosses = "max"` 設定經 LibreOffice headless 渲染確認完全沒效果，時間軸仍在下方。接續測試 `axPos="t"`、`tickLblPos="high"` 兩個語意上應該有效的 OOXML 屬性，皆同樣被忽略、渲染結果無變化。詢問使用者截圖來源軟體，確認是 LibreOffice；使用者進一步回報用 Google 試算表開啟同一檔案，軸線位置也在下方——確認這不是單一軟體的 bug，是「調整既有軸屬性換位置」這個手法本身在多數實作裡都不受支援。
- 改採使用者提出的方向：新增一組次座標時間軸（`axId=200`），與主座標共用同一組姓名類別軸，使其顯示在圖表上方；上下兩條時間軸皆保留（使用者選擇「上下都保留」而非只留一條）。`add_xlsx_gantt_chart()` 移除無效的 `crosses="max"` 單軸設定，改為 `chart += top_axis_chart` 合併第二張隱藏 series 的圖表。
- 驗證：LibreOffice headless 渲染確認上下皆顯示時間刻度且與長條圖對齊、姓名軸未重複；直接打正式 `/api/convert` API 解壓確認回應內容確實含 `axId="200"`；`tests/verify_engine.py` 全數 `healthy=True` 無回歸。使用者透過瀏覽器實測確認效果正確。
- 接續新需求：圖表標題除了「日」之外要加上月份，且月份需從班表檔案內容辨識（不能寫死），同步套用於網頁預覽與輸出檔案圖表。
- 與使用者確認關鍵架構決策：月份**不寫入範本 JSON**，每次上傳都重新從當次檔案內容判斷——因為範本設計上是跨月重複使用的，若寫死月份會導致下個月套用同一份範本時月份卡在舊值。
- 實作：`backend/heuristics.py` 新增 `guess_month()`，掃描日期列儲存格判斷月份（支援 datetime 物件與「N月M日」文字格式；純數字日期欄位偵測不到月份時回傳 `None`）；`backend/parser.py` 的 `parse_schedule()` 回傳月份；`backend/main.py` 的 `/api/preview`、`/api/convert` 帶出月份；`backend/generator.py` 的 `generate_xlsx()`/`add_xlsx_gantt_chart()` 接收月份組進標題；`frontend/app.js` 新增 `currentMonth` 狀態，`renderDayGantt()` 標題同步套用。
- 驗證：用 10 份範例／真實客戶檔案全數重跑月份判斷，7 月（白霧、範例1）、1 月（範例4/5、旺鍋、桃城雞排）、3 月（MR08）皆正確；純數字日期欄位（範例2/3、MOU班表模組）正確回傳 `None`，標題退回原樣不受影響。`tests/verify_engine.py` 全數 `healthy=True` 無回歸；直接打 API 確認 MR08 檔案標題顯示「3月1日 排班長條圖」。使用者透過瀏覽器實測確認成功。
- **附帶發現**：測試月份判斷時發現 `旺鍋甘特圖班表公版(時間至26).xlsx`（先前未測試的真實客戶檔案）完全解析不出員工資料（`healthy=False`），是既有欄位辨識問題，與本輪修改無關，列入待辦。
- 本階段結束，關閉本機測試 server、推送至 GitHub。

### 備忘事項

- 「調整輸出檔案的顯示格式」：本輪已完成「時間軸移至上方」與「標題加月份」兩項；若使用者後續還有其他版面調整需求，待提出。
- 白霧.ods、範例班表2/3/5 仍尚未經使用者本人親自操作確認（沿用既有待辦）。
- **新發現待辦**：`旺鍋甘特圖班表公版(時間至26).xlsx` 完全解析不出員工資料（`healthy=False`），既有欄位辨識問題，需要排查（詳見上方本輪紀錄）。
- 之後修改 `backend/*.py`：因 server 目前不帶 `--reload`，需手動關閉重啟；建議重啟後先自行打 API 驗證回應內容正確，再請使用者測試。

## 2026-07-22（續，新階段）

### 旺鍋公版檔案解析失敗排查（本輪，接續上方待辦）

- 直接用 Python 讀取 `data/旺鍋甘特圖班表公版(時間至26).xlsx` 的原始儲存格內容排查（`read_xlsx_rows()` + dump），發現每個員工區塊的「姓名」欄位填的字面上就是「姓名」兩個字（欄位標題本身，不是真人姓名），「員編」列全部是 0。
- 確認這份檔案是**從未填入實際員工資料的空白母版**（檔名「公版」＝通用空白範本，印證這點）；既有的 `is_name_like()` 正確把「姓名」判定為 `_HEADER_KEYWORDS` 標題關鍵字而排除，因此掃不到任何員工，`is_healthy=False`／「未解析出任何員工資料」是既有防呆邏輯的**預期行為，不是程式的 bug**，不需要修改程式碼。
- 已與使用者確認列為「已排查，非 bug」結案。

### 甘特圖新增「每小時上班人數」顯示（本輪重點，使用者新需求）

- 需求：xlsx 甘特圖新增每小時上班人數，顯示於時間軸上方。
- 統計方向評估：使用者提出兩個方向（資料驅動 vs 視覺計算圖表橫線數量），評估後確認**資料驅動**（沿用 `employees[i]["days"][d]["shifts"]` 已解析出的班別起訖時間做區間重疊計數）明顯較可靠——視覺計算等於把資料驅動已經算出來的答案，繞一條更間接、更容易受渲染引擎差異影響的路（影像辨識或重算圖表幾何座標）再算一次，沒有額外好處。使用者確認採用資料驅動，並確認小時計數規則為「區間重疊」（`[h, h+1)` 與班別重疊即算，非整點瞬時判斷）。
- 顯示機制確認：現有甘特圖是 `chart.type="bar"`（類別軸=姓名、數值軸=時間），評估後提出「做法A」（姓名軸最上方新增虛擬列「上班人數」，沿用甘特圖既有座標系統：固定 1 小時寬度區段＋自訂資料標籤顯示數字）優於「做法B」（脫離繪圖區飄在軸線外側，需手動換算 EMU 座標、無法保證跨軟體對齊，與先前兩次踩過的軸線位置坑同一類風險）。使用者確認採用做法A、方塊透明無色只顯數字、虛擬列標籤文字用「上班人數」。
- 實作：`backend/generator.py` 新增 `compute_hourly_headcount()`；`generate_xlsx()` 姓名軸第2列保留給人數虛擬列（真實員工資料下移一列，`r = idx + 3`）、新增每小時固定 1 小時長度（1/24）的隱藏輔助欄；`add_xlsx_gantt_chart()` 新增每小時人數 series（透明、自訂資料標籤），並修正 `chart_col` 欄位英文字母換算（新增欄位後容易超過 26 欄，原本 `chr(65+n)` 換算會產生非法欄位代號，改用既有的 `_col_letter()`）。
- **除錯（重要，避免下次重複繞路）**：LibreOffice headless 渲染發現兩個顯示問題：
  1. 姓名軸文字疊字亂碼（如「上班人數;cnt8;0.0417」）——根因是資料標籤沒有明確關閉 `showCatName`/`showSerName`，LibreOffice 對「有自訂標籤但未明確關閉這兩項」的預設行為是照樣顯示類別/數列名稱。且這個關閉必須設在 `dLbls`這一層（適用其他未被 idx=0 覆蓋的點，即真實員工那些人數欄=0 的點），否則會在真實員工的長條上疊出「Alice;cnt10;0」之類多餘文字。
  2. 人數數字沒顯示、只顯示原始小數（如 0.0416666666666667）——根因是 openpyxl 的 `DataLabel.numFmt` 只能設定 `formatCode`，**沒有介面可設定 OOXML 規格要求的 `sourceLinked` 屬性**；沒有明確標示 `sourceLinked="0"`，Excel／LibreOffice 會判定「沿用來源儲存格格式」而忽略自訂格式碼字面值。
  - 用手動 patch 測試檔（小規模 Alice/Bob 兩人對照組）驗證這兩個假設皆正確後，與使用者確認新增「存檔後 XML 後處理」步驟修復（`_fix_datalabel_numfmt_sourcelinked()`，正規表示式補上 `sourceLinked="0"`）——**這是這個專案第一次採用「存檔後修改 XML」的手法**（先前都只有讀取驗證，沒有寫入修改過）。
- 驗證：小規模對照組（Alice/Bob 兩人、8:00-12:00 四小時軸）與真實範例資料（5人／10人／30人）逐一 headless 渲染確認人數與長條對齊正確、無疊字亂碼；`tests/verify_engine.py` 6 份範例全數 `healthy=True` 無回歸（員工數 10/30/6/7/14/5 與既有基準一致）；啟動本機 server（不帶 `--reload`）直接打 `/api/convert` API 解壓確認正式流程輸出的 XML 也正確套用 `sourceLinked="0"`。
- 驗證完成後關閉本機測試 server、清理除錯過程產生的暫存檔案。

### 備忘事項

- **下次待辦（使用者已確認）**：網頁預覽（`frontend/app.js` 的 `renderDayGantt()`）尚未加上每小時上班人數，需要同步實作。網頁預覽是純 HTML/CSS/JS，不受 Excel 圖表限制，可直接用 DOM 文字對齊，預期不需要 xlsx 那套 numFmt/sourceLinked 的 workaround。
- 旺鍋公版檔案解析失敗已排查結案，確認非 bug，不需修復。
- 其餘既有待辦（部署方式討論、白霧.ods/範例班表2/3/5 使用者親自驗證、MOU／桃城雞排測試）沿用未變。

## 2026-07-23

### 每小時上班人數計算邏輯修正：半點班次比例計算（本輪，使用者回報問題）

- 開工先讀取交接文檔與 2026-07-22 工作日誌，啟動本機 server（port 8000，不帶 `--reload`），確認健康檢查正常後交使用者實測上輪完成的 xlsx「每小時上班人數」功能——**使用者確認顯示功能正確**。
- 使用者接著回報：計算邏輯有誤，真實班表會出現 9:30 這類非整點（半點）班次，該小時應計為 0.5 人，但目前邏輯（區間有重疊就整數 +1）算成 1。
- 修正 `backend/generator.py` 的 `compute_hourly_headcount()`：改為按「重疊時數比例」加總（`overlap = min(e, h+1) - max(s, h)`），非整點上下班時記為小數；同步調整 `add_xlsx_gantt_chart()` 的資料標籤格式化，整數不帶小數點、非整數才顯示小數。
- 驗證：Alice(9:30-17:00)/Bob(9:00-12:00) 對照組手算＋headless 渲染確認 9-10 點格顯示 `1.5` 且與 Alice 長條起點對齊；重跑 `tests/verify_engine.py` 六份範例全數 `healthy=True` 無回歸。

### 網頁預覽同步加上「每小時上班人數」（本輪，沿用上輪已確認待辦）

- `frontend/app.js` 新增 `computeHourlyHeadcount()`（與後端邏輯一致的重疊比例計算）與 `fmtHeadcount()`；`renderDayGantt()` 在員工列最上方插入「上班人數」虛擬列，對齊每小時刻度顯示數字。`frontend/style.css` 新增對應樣式，文字色用 `var(--text-primary)` 確保深/淺色主題對比度足夠。
- 前端無 build step，不需重啟 server；因 agent 環境無瀏覽器可實際操作，僅完成 JS/Python 計算結果比對一致（用真實 13 人班表資料交叉驗證）與語法檢查，請使用者本人瀏覽器實測。
- **使用者實測確認「網頁的橫條圖能正確顯示上班人數，修正成功」**。

### 真實 13 人班表（MR08）人數欄位疑似未修正排查（本輪，使用者截圖回報，最終確認非 bug）

- 使用者截圖回報：11:00-12:00 與 15:00-16:00 兩格仍顯示整數（4、3），懷疑這兩格半點邏輯沒套用，17:00-18:00／21:00-22:00 顯示小數（3.5／2.5）看起來才是修正生效。
- 直接用使用者實際檔案＋已存範本（`data/MR08裕隆城甘特圖班表3月.xlsx` + `templates/MORE/e59e7d978fa8.json`）呼叫修正後的 `compute_hourly_headcount()`，逐格算出的結果與截圖完全一致：`[1, 4, 7, 7, 6, 3, 1, 3.5, 6, 6, 5, 2.5]`。
- 逐項拆解算式向使用者說明：11-12／15-16 兩格恰好各有 **6 位**（偶數）員工只在該格內有半小時重疊，6×0.5=3.0，加總後剛好變回整數；17-18／21-22 則是 **5 位**（奇數）員工半小時重疊，5×0.5=2.5，才會顯示看得出來的小數。並非「兩格漏套用修正」，而是同一套重疊比例邏輯在偶數人數時剛好加總成整數，純數學巧合，非程式邏輯遺漏。
- **使用者確認「你目前邏輯是正確的，也是我想要的，是我剛剛檢查時錯誤了」**，結案。

### 已存範本流程調整：選擇已存範本應跳過確認欄位視窗（本輪，使用者提出新需求）

- 使用者截圖回報：上傳頁選擇已存範本（如「MORE範本」）並點擊「分析檔案」後，仍會跳出「確認欄位對照」視窗要求手動核對，使用者認為既然範本已存，理應直接進入甘特圖預覽畫面，這才是「儲存範本」省去重複確認的意義。
- 確認後端 `/api/analyze` 為無副作用的純解析端點（不寫入任何伺服器狀態），可以在已選定已存範本時完全跳過呼叫。
- 重構 `frontend/app.js`：抽出共用函式 `runPreview(template)`（呼叫 `/api/preview`、更新 `confirmedTemplate`/`lastEmployees`/`currentMonth`、顯示甘特圖預覽面板），供「已存範本」與「確認視窗按下確認」兩處呼叫。`analyzeBtn` 點擊流程改為：若下拉選單已選「已存範本」，直接讀取該範本 JSON 後呼叫 `runPreview()`，完全跳過 `/api/analyze` 與確認欄位視窗；未選已存範本才維持原本「分析→開啟確認視窗」流程。
- 驗證：JS 語法檢查通過；直接打 `GET /api/templates/MORE/e59e7d978fa8` 確認範本讀取端點正常回應。同樣因 agent 環境無瀏覽器，請使用者本人操作確認。
- **使用者實測確認「流程已修改正確」**，結案。

### 本階段結束

- 本輪四項問題／需求（半點人數計算修正、網頁預覽同步顯示、MR08 疑似未修正排查、已存範本跳過確認視窗）皆已由使用者本人操作驗證通過，**本階段無未完成事項**。
- 更新交接文檔／工作日誌／對話紀錄，推送至 GitHub，關閉本機測試 server。

## 2026-07-23（同日第二輪：部署上線）

### 部署方式討論與 Render 上線（本輪，使用者要求進入部署階段）

- 使用者要求討論部署方案（慣用 github/render/vercel），並要求說明先前提過但未展開的 Docker。分析後建議單一 Render Web Service 同時承載前端靜態檔＋FastAPI API（沿用現有架構不拆分），理由：Vercel serverless 無持久化磁碟，與本專案「範本存本機檔案系統」機制不相容。說明 Docker 非部署必要條件（依賴極輕，Render 原生 Python 路線即可），價值在未來平台整合的可攜性。
- 使用者決策：範本持久化本階段可接受 demo 消失（不用付費 Persistent Disk）；本次不寫 Dockerfile，用 Render 原生 Python 路線；Render 設定寫成 `render.yaml` 存進 repo。
- 新增 `render.yaml`（build/start command，釘選 `PYTHON_VERSION: 3.11.9`，因本機為過新的 3.14.5、程式碼未用新語法，選穩定廣泛支援版本）。使用者於 Render 手動建立服務、貼上 build/start command 完成部署，`/api/health` 回應正常，上線網址：`https://shift-schedule-b5ne.onrender.com`。

### 使用手冊（HTML 版）與工具安裝（本輪，使用者要求）

- 使用者要求撰寫給完全沒接觸過系統的使用手冊（含操作流程＋Q&A）。環境內未安裝 python-docx/python-pptx/pandoc，告知目前可產生格式（.docx 現場安裝／HTML／Markdown），建議不用 PPT（內容形式不匹配）。
- 使用者選擇本次以 HTML 呈現，並要求安裝 `python-docx`／`python-pptx` 供本專案與未來其他專案使用——**已全域 `pip install` 完成**。
- 依 `frontend/index.html` 實際欄位/按鈕文字撰寫手冊（三步驟流程＋9 則常見問題，涵蓋檔案格式限制、20MB 上限、已存範本行為、上班人數計算邏輯、範本目前無權限驗證等誠實揭露），視覺沿用產品既有色票（accent 藍 `#2a78d6`）。先發布 Claude Artifact 預覽，因 Artifact 預設私人連結不適合放公開 README，使用者決定改自行主機為 `frontend/manual.html`，跟隨既有 FastAPI 靜態掛載機制部署。

### README 新增 Demo 區塊並推送（本輪，使用者要求）

- 依規則的 README 欄位順序（Demo → 專案目標 → 計畫架構 → 已完成進度 → 未完成事項），新增「## Demo」區塊：Demo 連結（Render 網址）＋使用手冊連結（`/manual.html`）兩顆徽章按鈕，並註明 Free tier 閒置喚醒提醒。
- `git add` README.md／frontend/manual.html／render.yaml（排除未追蹤的 `debug_server.log`），commit 並 push 成功（`869dbfe..9829813`）。
- **提醒但未修改**：README「未完成事項」內「部署方式：尚未決定」已過時（部署已完成），因使用者本輪未要求更新該段落內容，依規則未主動修改，僅口頭告知使用者。

### 本階段結束

- 本輪三項工作（Render 部署上線、使用手冊 HTML 版、README Demo 區塊）已完成並推送，使用者尚未回報任何驗證失敗。
- **本階段無未完成事項**（沿用待辦：README「未完成事項」段落文字更新，非阻塞，待使用者日後要求）。
- 本機測試 server 確認未在執行中（`curl 127.0.0.1:8000/api/health` 連線失敗），無需額外關閉。已更新交接文檔／工作日誌／對話紀錄，推送至 GitHub。

## 2026-07-23（同日第三輪：README 未完成事項更新、系統架構檢視、四項修正）

### README「部署方式」逐項更新為已定案狀態（本輪，使用者要求）

- 使用者確認範本持久化決策：demo 階段接受 Free tier 消失、商轉後升級付費方案排除。更新 README「部署方式」段落並推送。
- 使用者詢問「存取控制」問題具體內容，說明 `scope_id` 無歸屬驗證的實際風險（範本內容非員工個資、僅欄位對照設定；CORS 全開與此是同一風險非疊加）與受限於「不自建帳號系統」既有架構決策的可選方向。使用者決策：demo 階段暫不處理，待接入餐飲平台主體時 `scope_id` 改由帳號密碼機制產生。更新 README 並推送。

### 系統架構檢視（本輪，使用者要求）

- 使用者要求檢視是否還有其他系統架構問題。通讀 `backend/main.py` 全部端點後列出四項發現：(1) API 無 rate limiting（`/api/analyze`／`/api/convert` 等端點無呼叫頻率限制）；(2) 例外訊息直接回傳 Python 例外內容給前端；(3) 無伺服器端 log／可觀測性；(4) 同步阻塞運算寫在 `async` 路由中（次要）。使用者要求先記錄進 README 未完成事項，再逐項討論。

### 討論並實作 Rate Limiting（本輪）

- 使用者提出「5 次/5 分鐘」的限流構想，本輪先討論：指出此門檻可能誤傷正常使用者（單一使用者一次完整流程本身就可能達 4~8 次呼叫；同店多人共用 IP 會共用額度），建議改為「只限流真正吃資源的端點（analyze/preview/convert）、門檻設寬作為濫用斷路器」而非一般使用配額。
- 使用者採納建議，要求實作並提供正常使用實際呼叫次數。逐一追蹤 `frontend/app.js` 的呼叫序列，確認：新分析流程約 4~6 次、已存範本流程約 3 次。安裝 `slowapi`，對 `/api/analyze`／`/api/preview`／`/api/convert` 加上 20 次/分鐘（依 client IP）限制，`/api/templates` CRUD 與 `/api/health` 不限流。發現並修正一個關鍵細節：Render 邊緣代理若不處理會導致 `slowapi` 抓到代理 IP 而非真實使用者 IP，比使用者原本擔心的「同店共用 IP」更嚴重（全部使用者共用同一組額度）；於 `render.yaml` 的 startCommand 加上 `--proxy-headers` 修正，並提醒使用者需手動同步更新 Render 網頁上的 Start Command 欄位。
- 驗證：用 `TestClient` 連續呼叫 `/api/analyze` 21 次，確認第 21 次起收到 429；`/api/health` 連續 30 次不受影響；`tests/verify_engine.py` 六份範例迴歸測試無回歸。已推送。

### 修正例外訊息外洩（本輪，使用者要求直接修正並確認是否為 debug 殘留）

- 使用者要求直接修正、並詢問這是否為先前 debug 時殘留。查 git log 確認這三行例外處理自 2026-07-15 建立後端的第一個 commit 就存在，**非 debug 殘留**，是原始設計（想給使用者欄位對照錯誤的線索），但沒有篩選內容直接回傳。
- 修正：保留對使用者有意義的提示文字（如「請確認範本欄位對照是否正確」），拿掉原始 Python 例外內容；真實例外改用 `logging.exception()` 寫伺服器端 log（Render 自動收集 stdout/stderr）。用 `TestClient` 分別觸發三處例外路徑（含真實範例檔案＋錯誤欄位對照），確認伺服器端印出完整 traceback、回應內容不含檔案路徑/堆疊片段。已推送。

### README 未完成事項收斂（本輪，使用者確認多項已解決）

- 使用者確認：甘特圖姓名軸標籤跳過問題已排除、輸出格式調整已完成，兩項移出未完成事項；員編門檻／3 段班驗證保留但重新表述為「非 bug，待補充更多真實資料」；rate limiting 與例外訊息外洩兩項移至已完成進度；無伺服器端 log 一項改為「部分改善」（僅涵蓋三處例外路徑，非全面請求層級 log）。已推送。

### 討論並修正同步阻塞運算問題（本輪，使用者要求說明討論）

- 使用者要求說明第 4 項（同步阻塞運算寫在 async 路由）。解釋 FastAPI/uvicorn 單一事件迴圈模型：`async def` 路由若內部是同步阻塞運算（`openpyxl` 解析/產生圖表）會佔住整條事件迴圈、拖累所有其他請求；`def`（非 async）路由則會被自動丟進執行緒池不受影響。查證 `/api/analyze`／`/api/preview`／`/api/convert` 三個函式雖宣告 `async def` 但內部完全沒有 `await`，等於承受最差組合卻無 async 的好處。指出這項修法幾乎零成本零副作用（只需拿掉 `async` 關鍵字），但以目前使用規模（少數店家、非高併發）影響很小，屬於四項中優先度最低者。
- 使用者確認處理。將三個函式改為一般 `def`。驗證：`TestClient` 重跑 rate limit 測試（429 行為不變）、例外訊息測試（篩選機制不變）、三端點成功路徑端對端測試（真實範例檔案：分析→試解析→下載，含 xlsx 位元組數確認），以及 `tests/verify_engine.py` 六份範例迴歸測試，全數通過無回歸。README 更新已完成進度並推送。

### 本階段結束

- 本輪工作：README 部署方式/存取控制決策定案、系統架構檢視（四項發現）、rate limiting 實作、例外訊息外洩修正、同步阻塞運算修正、README 未完成事項全面收斂。皆已推送並經 TestClient/回歸測試驗證。
- **本階段無未完成事項**。剩餘 README 未完成事項（部署方式的範本持久化/存取控制觸發條件、員編門檻/3段班驗證待補資料、無伺服器端 log 的部分改善）皆為已知、非阻塞、待未來條件成立或資料累積後處理。
- 本機測試 server 確認未在執行中，無需關閉。

